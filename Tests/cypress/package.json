{
  "scripts": {
    "test": "set CYPRESS_CURRENT_ENV=test&& npx cypress run",
    "test:local": "set CYPRESS_CURRENT_ENV=local&& npx cypress run",
    "testOpen": "set CYPRESS_CURRENT_ENV=local&& npx cypress open --browser chrome"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "cypress": "^12.7.0",
    "mochawesome": "^7.1.3"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0"
  }
}
